[blockchain]
blockchain_cli = "verus -chain=chips"   # Local VRSCTEST regtest
currency       = chips                  # Native currency name used in sendcurrency (defaults to "chips" if missing)
new_block      = "Y"                       # Cashier processes payin_tx requests on every new block
