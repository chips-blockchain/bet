[blockchain]
blockchain_cli       = "verus -chain=chips"          #This is the blockchain client, it can be configured any blockchain client that bet supports, currently bet supports native chips and chips chain on verus. If its left empty the default blockchain client be used by bet which is native chips client. 

