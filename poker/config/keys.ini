# Verus IDs Configuration (local VRSCTEST regtest)
# Identity names must be fully-qualified Verus IDs (i.e. include the trailing
# '@' and parent suffix). Short names are rejected.

[identities]
# Cashier aggregator identity (holds cashier list)
cashier_id = cashier.sg777z.chips.vrsc@

# Dealer aggregator identity (holds dealer list)
dealers_id = dealers.sg777z.chips.vrsc@
